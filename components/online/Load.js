import st from '../../styles/online/Load.module.css'

const Load = () => {
    return (
        <div className={st.loadContainer}>
            <button className={st.loadMoreButton}>Load More</button>
        </div>
    )
}

export default Load
